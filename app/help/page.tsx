import { Card } from '@/components/ui/card'
export default function HelpPage() {
  return <div className="mx-auto max-w-4xl px-4 py-10"><h1 className="text-3xl font-semibold">Help & pilot guide</h1><p className="mt-2 text-muted-foreground">A short map of what NetworkMind can—and deliberately cannot—do.</p><div className="mt-7 grid gap-4 sm:grid-cols-2"><Card className="p-5"><h2 className="font-semibold">Importing contacts</h2><p className="mt-2 text-sm text-muted-foreground">Use a CSV with a name or first/last-name columns. Review every automatic mapping and row warning before confirming.</p></Card><Card className="p-5"><h2 className="font-semibold">Asking questions</h2><p className="mt-2 text-sm text-muted-foreground">Network mode searches only stored records. Web mode is clearly labelled and requires web enrichment. Mixed mode keeps public and private evidence separate.</p></Card><Card className="p-5"><h2 className="font-semibold">Introductions</h2><p className="mt-2 text-sm text-muted-foreground">A named introduction is shown only when a verified stored relationship edge exists. Similar industries, employers, or topics are not proof.</p></Card><Card className="p-5"><h2 className="font-semibold">Privacy</h2><p className="mt-2 text-sm text-muted-foreground">External processing is configurable in Settings. Network-only mode prevents web calls for a question. This pilot needs formal security and legal review before broader use.</p></Card></div></div>
}
